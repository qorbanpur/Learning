@model IEnumerable<MVCLearning.Models.Employee>

<p>
    @Html.Action(actionName: "ShowMenu", controllerName: "Menu", routeValues: new { options = "Something from somewhere... (Applying Html.Action on a miscellaneous PartialView)" })
    <br />
    @{Html.RenderAction(actionName: "ShowMenu", controllerName: "Menu", routeValues: new { options = "Something from somewhere... (Applying Html.RenderAction on a miscellaneous PartialView)" }); }
    <br />
    @{Html.RenderPartial(partialViewName: "_ShowMenu", model: "Something from somewhere... (Applying Html.RenderPartial on a miscellaneous PartialView)");}
    <br />
    <br />
    @Html.ActionLink("Create New", "Create_UsingAutoGeneratedForm")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salary)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Password)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsMale)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AddDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsAdmin)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    @*@@Html.RenderPartial("_EmployeeItem", item);*@
    @Html.Partial("_EmployeeItem", item);
}

</table>
